/**
 * Drizzle schema root.
 *
 * No entities exist yet — they arrive in later issues (User, RefreshToken,
 * Project). This file is the single place every table definition gets
 * re-exported from, so `drizzle-kit` and the runtime always see the same set.
 */
export {};
