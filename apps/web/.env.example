# origem exata do site, sem barra final e sem caminho -- usada em
# metadataBase, sitemap.xml e og:image (ver lib/site.ts)
FRONTEND_URL=http://localhost:3101
# fine-grained Personal Access Token, "Public Repositories (read-only)", sem
# permissoes extras -- ver docs/decisao-projetos-github-pins.md
GITHUB_TOKEN=
# usuario do GitHub cujos pinned repos aparecem em /projetos e na home
GITHUB_USERNAME=
