node_modules
dist
.next
coverage
pnpm-lock.yaml

# regenerated by Next on every dev/build run, and gitignored — formatting it
# only makes format:check fail locally for anyone who has run the app
next-env.d.ts

# prose maintained by hand, not code — out of scope for auto-formatting
.claude
docs
*.md

# hand-maintained root config, untouched by this sub-issue
pnpm-workspace.yaml
